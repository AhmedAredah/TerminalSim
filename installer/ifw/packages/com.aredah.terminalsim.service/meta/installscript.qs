// Service component install script. Files are placed; activation is left to
// the operator (documented in the README).

function Component() {}

Component.prototype.createOperations = function () {
    component.createOperations();
};
